 # Cria uma rede de conexão entre dois containers. (OBS: deve ser criado antes dos containers)
sudo docker network create library-network

 # Rodar container Postgres na versão 16.3
sudo docker run --name librarydb -p 5432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -e POSTGRES_DB=library --network library-network -d postgres:16.3

# Rodar Pgadmin 4
sudo docker run --name pgadmin4 -p 15432:80 -e PGADMIN_DEFAULT_EMAIL=joaoleal98@outlook.com -e PGADMIN_DEFAULT_PASSWORD=admin --network library-network -d dpage/pgadmin4


 # Comando utilitários:
  # Verificar containers inicializados
sudo docker ps

 # Verificar a lista de todos os containers no host
sudo docker ps -a

 # Deletar o container
sudo docker container rm <nome_container>
sudo docker rm <nome_container>

 # Forçar a deleção do container
sudo docker container rm -f <<nome_container>>
sudo docker rm -f <nome_container>

 # Iniciar o container
sudo docker start <nome_container>

 # Parar o container
sudo docker stop <nome_container>

 # Verificar imagens
sudo docker images

# Limpe o contexto de construção (caso use o docker desktop pois pode haver problemas de contexto)
docker builder prune

 # Verificar logs (nome do container) - esse comando roda a aplicação
sudo docker logs libraryapi-production


 # Para construir e executar um container que use variáveis de ambiente
docker run --env-file ./env.vars joaolealdev/libraryapi
 # outro exemplo
sudo docker run --env-file ./env.vars -p 8080:8080 -p 9090:9090 joaolealdev/libraryapi

 ---
 ###### Production ######

sudo docker run --name librarydb-prod -e POSTGRES_USER=postgresprod -e POSTGRES_PASSWORD=postgresprod -e POSTGRES_DB=library -p 5433:5432 -d --network library-network postgres:16.3

 # comando para inicializar container apartir da imagem(/joaolealdev/libraryapi):sudo docker run --name libraryapi-production -e DATASOURCE_URL=jdbc:postgresql://librarydb-prod:5432/library -e DATASOURCE_USERNAME=postgresprod -e DATASOURCE_PASSWORD=postgresprod --network library-network -d -p 8080:8080 -p 9090:9090 joaolealdev/libraryapi

